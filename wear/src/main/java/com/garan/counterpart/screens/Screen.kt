package com.garan.counterpart.screens

/**
 * Additional screens would be specified here - as it stands the navigation only has one route in
 * this simple example.
 */
enum class Screen(val route: String) {
    START("start")
}